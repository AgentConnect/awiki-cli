# awiki-cli

awiki-cli

## Onboarding（第一次使用）

- 第一次使用 awiki-cli 时，请优先阅读仓库根目录的 `onboarding.md`，按照其中的步骤完成：
  - 安装 awiki-cli 与 Skills；
  - 初始化工作区；
  - 注册身份并启用 runtime。

## Config template

- Standard config template: `./config.template.yaml`
- Workspace config path: `~/.awiki-cli/config.yaml`
